<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Purposed4</title>
  <style>
    :root {
      --bg: #f5f7fb;
      --panel: #ffffff;
      --panel-2: #eef2ff;
      --text: #142033;
      --muted: #64748b;
      --border: #e2e8f0;
      --accent: #111827;
      --accent-2: #2b3a67;
      --user: #111827;
      --user-text: #ffffff;
      --ai: #f1f5f9;
      --shadow: 0 12px 40px rgba(15, 23, 42, 0.08);
      --radius: 18px;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: Inter, Arial, sans-serif;
      background:
        radial-gradient(circle at top left, #eef2ff 0%, transparent 35%),
        radial-gradient(circle at top right, #e0f2fe 0%, transparent 30%),
        var(--bg);
      color: var(--text);
    }

    .page {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
    }

    .shell {
      width: 100%;
      max-width: 1100px;
      display: grid;
      grid-template-columns: 320px 1fr;
      gap: 20px;
    }

    .sidebar,
    .chat-shell {
      background: var(--panel);
      border: 1px solid var(--border);
      border-radius: 24px;
      box-shadow: var(--shadow);
    }

    .sidebar {
      padding: 28px 24px;
      display: flex;
      flex-direction: column;
      gap: 22px;
    }

    .brand {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .brand-mark {
      width: 48px;
      height: 48px;
      border-radius: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: var(--accent);
      color: white;
      font-weight: 700;
      font-size: 18px;
    }

    .brand h1 {
      margin: 0;
      font-size: 28px;
      line-height: 1.1;
    }

    .brand p {
      margin: 0;
      color: var(--muted);
      line-height: 1.6;
      font-size: 14px;
    }

    .sidebar-card {
      background: #f8fafc;
      border: 1px solid var(--border);
      border-radius: var(--radius);
      padding: 16px;
    }

    .sidebar-card h3 {
      margin: 0 0 8px;
      font-size: 15px;
    }

    .sidebar-card p {
      margin: 0;
      color: var(--muted);
      line-height: 1.6;
      font-size: 14px;
    }

    .pathways {
      display: grid;
      gap: 10px;
    }

    .pathway {
      padding: 12px 14px;
      border: 1px solid var(--border);
      border-radius: 14px;
      background: white;
    }

    .pathway strong {
      display: block;
      font-size: 14px;
      margin-bottom: 4px;
    }

    .pathway span {
      color: var(--muted);
      font-size: 13px;
      line-height: 1.5;
    }

    .chat-shell {
      overflow: hidden;
      display: flex;
      flex-direction: column;
      min-height: 760px;
    }

    .chat-header {
      padding: 22px 24px;
      border-bottom: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: linear-gradient(to right, #ffffff, #f8fafc);
    }

    .chat-header h2 {
      margin: 0;
      font-size: 18px;
    }

    .chat-header p {
      margin: 4px 0 0;
      color: var(--muted);
      font-size: 13px;
    }

    .status {
      font-size: 12px;
      color: #0f766e;
      background: #ecfeff;
      border: 1px solid #bae6fd;
      padding: 8px 10px;
      border-radius: 999px;
      white-space: nowrap;
    }

    .messages {
      flex: 1;
      overflow-y: auto;
      padding: 24px;
      background:
        linear-gradient(180deg, #fcfdff 0%, #f8fafc 100%);
    }

    .message-row {
      display: flex;
      margin-bottom: 18px;
    }

    .message-row.user {
      justify-content: flex-end;
    }

    .bubble {
      max-width: min(78%, 720px);
      padding: 14px 16px;
      border-radius: 18px;
      line-height: 1.65;
      font-size: 15px;
      white-space: pre-wrap;
      box-shadow: 0 4px 12px rgba(15, 23, 42, 0.04);
    }

    .message-row.ai .bubble {
      background: var(--ai);
      color: var(--text);
      border-bottom-left-radius: 6px;
    }

    .message-row.user .bubble {
      background: var(--user);
      color: var(--user-text);
      border-bottom-right-radius: 6px;
    }

    .composer-wrap {
      border-top: 1px solid var(--border);
      background: white;
      padding: 16px;
    }

    .composer {
      display: flex;
      gap: 12px;
      align-items: flex-end;
      background: #f8fafc;
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 10px;
    }

    .composer textarea {
      flex: 1;
      border: 0;
      outline: none;
      background: transparent;
      resize: none;
      min-height: 56px;
      max-height: 180px;
      padding: 8px 10px;
      font: inherit;
      color: var(--text);
      line-height: 1.5;
    }

    .composer button {
      border: 0;
      border-radius: 14px;
      background: var(--accent);
      color: white;
      padding: 14px 18px;
      font: inherit;
      font-weight: 600;
      cursor: pointer;
      transition: transform 0.15s ease, opacity 0.15s ease;
    }

    .composer button:hover {
      transform: translateY(-1px);
    }

    .composer button:disabled {
      opacity: 0.6;
      cursor: not-allowed;
      transform: none;
    }

    .composer-note {
      margin-top: 10px;
      color: var(--muted);
      font-size: 12px;
      text-align: center;
    }

    @media (max-width: 920px) {
      .shell {
        grid-template-columns: 1fr;
      }

      .sidebar {
        order: 2;
      }

      .chat-shell {
        min-height: 78vh;
      }
    }
  </style>
</head>
<body>
  <div class="page">
    <div class="shell">
      <aside class="sidebar">
        <div class="brand">
          <div class="brand-mark">P4</div>
          <h1>Purposed4</h1>
          <p>
            A guided space for reflection, direction, and purpose.
          </p>
        </div>

        <div class="sidebar-card">
          <h3>How this works</h3>
          <p>
            Start by sharing what feels most stuck right now. This conversation is designed to help bring clarity to what may already be taking shape within you.
          </p>
        </div>

        <div class="sidebar-card">
          <h3>Common areas</h3>
          <div class="pathways">
            <div class="pathway">
              <strong>Clarity</strong>
              <span>Direction, uncertainty, inner conflict, major decisions.</span>
            </div>
            <div class="pathway">
              <strong>Living</strong>
              <span>Stability, finances, housing, pressure, rebuilding life structure.</span>
            </div>
            <div class="pathway">
              <strong>Occupations</strong>
              <span>Work, purpose, contribution, fulfillment, career direction.</span>
            </div>
          </div>
        </div>
      </aside>

      <main class="chat-shell">
        <div class="chat-header">
          <div>
            <h2>Guided Conversation</h2>
            <p>Move at your own pace.</p>
          </div>
          <div class="status">Live conversation</div>
        </div>

        <div id="messages" class="messages"></div>

        <div class="composer-wrap">
          <div class="composer">
            <textarea id="input" placeholder="Share what feels most stuck right now..."></textarea>
            <button id="sendBtn">Send</button>
          </div>
          <div class="composer-note">
            This space is designed for reflection, clarity, and next-step insight.
          </div>
        </div>
      </main>
    </div>
  </div>

  <script>
    const messages = document.getElementById("messages");
    const input = document.getElementById("input");
    const sendBtn = document.getElementById("sendBtn");

    function addMessage(text, type) {
      const row = document.createElement("div");
      row.className = `message-row ${type}`;

      const bubble = document.createElement("div");
      bubble.className = "bubble";
      bubble.textContent = text;

      row.appendChild(bubble);
      messages.appendChild(row);
      messages.scrollTop = messages.scrollHeight;
    }

    async function sendMessage() {
      const text = input.value.trim();
      if (!text) return;

      addMessage(text, "user");
      input.value = "";
      sendBtn.disabled = true;

      try {
        const res = await fetch("/chat", {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
          },
          body: JSON.stringify({ message: text })
        });

        const data = await res.json();

        if (!res.ok) {
          addMessage(`Error: ${data.error || "Something went wrong."}`, "ai");
        } else {
          addMessage(data.reply, "ai");
        }
      } catch (error) {
        addMessage("Error: Unable to reach the server.", "ai");
      } finally {
        sendBtn.disabled = false;
        input.focus();
      }
    }

    sendBtn.addEventListener("click", sendMessage);

    input.addEventListener("keydown", (e) => {
      if (e.key === "Enter" && !e.shiftKey) {
        e.preventDefault();
        sendMessage();
      }
    });

    addMessage(
      "Welcome to Purposed4.\n\nTake a moment to think about your current season of life.\n\nWhat part of your life feels like it could use more clarity right now?",
      "ai"
    );
  </script>
</body>
</html>
